from setuptools import setup

setup(
      name = "ots.pyro_clients",
      author = "ext-teemu.a.vainio@nokia.com",
      version =  0.1,
      include_package_data = True,
      packages = ['ots',
                  'ots.pyro_clients'],
      zip_safe = False,
      )
